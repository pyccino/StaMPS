#!/bin/sh
# Thin shim: delegates to the Python implementation.
# To revert to the original csh behavior, set STAMPS_LEGACY_CSH=1.
if [ "${STAMPS_LEGACY_CSH:-0}" = "1" ]; then
    exec "$(dirname "$0")/legacy-csh/mt_extract_cands" "$@"
fi
exec python3 -m stamps.mt_extract_cands "$@"
